services:
  api:
    build:
      context: ../..
      dockerfile: apps/api/Dockerfile
    ports:
      - "3101:3101"
    environment:
      NODE_ENV: production
      PORT: 3101
      HOST: 0.0.0.0
      WEB_ORIGIN: http://localhost:3100
